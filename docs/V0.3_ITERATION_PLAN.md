# MetricLore v0.3 迭代计划

> 版本主题：Bring Your Own Data — 接入自己的数据，注册自己的模型，让 AI 为你问数。

## 1. 文档状态

- 目标版本：`v0.3.0`
- 建立日期：2026-08-27
- 当前阶段：M0 待开始
- 总体状态：In progress
- 维护规则：每个阶段完成后更新本文件的阶段状态、实际交付、验证结果、已知问题和提交号；推送远端后暂停，等待确认再进入下一阶段。
- 前置：v0.2（M0–M6）已全部完成并发布，见 [`V0.2_ITERATION_PLAN.md`](V0.2_ITERATION_PLAN.md)。

## 2. 版本目标

v0.3 的目标是把产品从"演示闭环"推进到"可自用闭环"。任何人都可以：

```text
克隆项目 → 配置自己的 API Key → 自建知识库（v0.2 已支持）
  → 上传自己的数据（CSV / Excel）
  → 注册自己的语义模型与指标
  → 让 AI 自动选择模型问数、分析
  → 用评测验证自己数据的准确率与回答质量
```

v0.2 已经打通了知识侧（文档 → Wiki → 审核 → 发布 → 检索）；v0.3 补齐数据侧缺失的中间层：**数据接入、数据工作区、语义模型注册闭环，以及让 AI 在多模型之间自动选择**。

## 3. 现状与缺口

| 环节 | 现状 | v0.3 目标 |
| --- | --- | --- |
| 自建知识库 | ✅ 完整闭环 | 保持 |
| 配置 API Key | ✅ 支持 | 保持 |
| 上传自己的数据（CSV/Excel） | ❌ 无入口 | 上传建表，自动推断 + 可微调 |
| 连接外部数据库 | ❌ 无入口 | 不在 MVP（待办） |
| 注册语义模型 | ⚠️ 只能选本地已有表 | 数据表 → 一键建模型 |
| 注册指标 | ⚠️ 依赖已有表 | 随语义模型闭环自然可用 |
| 问数使用的模型 | ⚠️ 只能手动切换"当前模型" | AI 跨模型自动路由 |

## 4. 设计原则

1. **数据上传不等于放开任意 SQL。** 上传生成的事实表仍然只能通过语义层访问；模型与提示词都不能绕过注册指标直接查询。
2. **安全建表。** 用户提供的表名、列名只作为业务名保存，物理表使用内部安全标识（`user_<uuid>`）；类型推断结果在上传预览中可微调，确认后才建表。
3. **删除保护。** 被语义模型引用的数据源禁止删除，提示先删除其模型，避免悬空引用。
4. **自动路由有边界。** AI 只在能明确判断时自动选择模型：术语唯一命中某模型即自动路由；多模型命中时优先当前会话/当前模型；仍有多义才澄清，不猜测。
5. **本地优先。** 上传的数据只保存在本地 SQLite，不发送到任何外部服务；LLM 相关能力仍需用户显式配置 API Key。
6. **信任可验证。** 用户自己注册的模型同样进入数据准确率评测与运行快照，让"我自己的数据准不准"可检查。

## 5. 功能范围

### 5.1 数据工作区（Data Workspace）

- 左侧导航新增「数据」入口。
- 数据源列表：内置表（`daily_metrics`）与用户数据源，展示行数、列数、来源与创建时间。
- 数据源详情：前 N 行预览、列类型、候选时间字段与维度、注册状态。
- 删除数据源：被语义模型引用时禁止删除并给出指引。
- 提供一键体验示例：内置一份示例 CSV，让用户 1 分钟走通"上传 → 建模型 → 问数"。

### 5.2 CSV / Excel 上传建表

- 支持 CSV（RFC 4180）与 XLSX。
- 自动推断：数值/整数/文本/日期列、时间字段识别、文本列作为候选维度。
- 上传预览可微调：列名、列类型、时间字段、维度勾选。
- 确认后建表并批量写入，限制上传大小、行数与列数。
- 复用 v0.1 已有的 CSV/XLSX 解析与 multipart 上传能力，不引入新依赖。

### 5.3 语义模型注册闭环

- 数据源详情页提供「基于此表创建语义模型」：自动预填时间字段与维度候选，沿用现有注册流程。
- 模型建好后引导注册第一个指标（原子或派生），随后即可问数。
- 用户旅程：上传数据 → 建模型 → 注册指标 → 提问 → 评测。

### 5.4 评测联动

- `data-accuracy` 套件对**当前用户模型**独立重算比对（逻辑已具备，补齐对自定义表/模型的覆盖）。
- 用户模型的 schema 纳入评测运行快照与历史，可回溯对比。
- 数据工作区提供该模型的评测结果入口。

### 5.5 多模型自动路由

- 指标/维度术语跨**所有**语义模型匹配，命中返回 `(modelId, metricKey)`。
- 唯一命中 → 自动路由到该模型；多模型命中 → 优先当前会话/当前模型；仍有多义 → 澄清。
- 多轮上下文记录 `modelId`，追问继承上一轮模型。
- 语义查询工具接受 `modelId`（底层 `buildQuery` 已支持，补齐解析与工具传递）。
- LLM 模式下目录工具展示全部模型，模型可自行选择；确定性解析作为兜底。
- 保留「当前模型」作为默认与评测基准。

## 6. 已确认的设计决策

1. **删除保护**：被语义模型引用的数据源禁止删除（提示先删除模型）。✅ 已确认
2. **一键体验示例**：数据工作区放置内置示例 CSV。✅ 已确认
3. **建表交互**：自动推断 + 可微调。✅ 已确认
4. **数据接入形态**：MVP 只做 CSV/Excel 上传；SQLite 文件导入、DuckDB 与网络数据库连接器整体后置到待办。✅ 已确认

## 7. 目标接口

```text
POST   /api/data/sources            # multipart 上传 CSV/XLSX，自动推断并建表
GET    /api/data/sources            # 数据源列表
GET    /api/data/sources/:id        # 详情 + 预览 + 推断结果
PATCH  /api/data/sources/:id        # 应用微调后的建表参数（或随 POST 提交）
DELETE /api/data/sources/:id        # 删除数据源（被引用时 409）

POST   /api/semantic/models         # 既有：基于数据表注册语义模型
POST   /api/semantic/models/:id/activate   # 既有：设为当前模型
POST   /api/semantic/metrics        # 既有：注册指标（携带 modelId）
GET    /api/catalog?modelId=        # 既有：按模型获取目录
POST   /api/query                   # 既有：语义查询（新增可选 modelId）

GET    /api/evaluation              # 既有：评测报告、运行、快照（纳入用户模型）
```

现有 `/api/knowledge/*`、`/api/conversations/*`、`/api/wiki/*` 接口保持兼容。

## 8. 阶段计划

| 阶段 | 目标 | 计划时间 | 状态 |
| --- | --- | ---: | --- |
| M1 | 数据工作区 + CSV/Excel 上传建表 | 4–6 天 | Completed |
| M2 | 语义模型注册闭环（数据表 → 建模型 → 注册指标） | 2–3 天 | Completed |
| M3 | 评测联动（data-accuracy 覆盖用户模型） | 2–3 天 | Completed |
| M4 | 多模型自动路由（跨模型匹配、上下文 modelId、澄清） | 3–5 天 | Completed |
| M5 | 文档、示例、演示与 v0.3.0 发布检查 | 2–3 天 | Completed |

### M1：数据工作区与上传建表

交付：

- `data_sources` 表与迁移。
- CSV/XLSX 上传、类型推断、预览与可微调、建表与批量写入。
- 数据源列表、详情、预览与删除（引用保护）。
- 一键体验示例 CSV。
- 数据工作区前端页面。

验收：用户可从零上传一份 CSV/Excel，在预览中调整列类型与维度后成功建表；表在数据源列表可见、可预览、可删除；被语义模型引用的表删除被阻止；示例 CSV 一键走通上传。

### M2：语义模型注册闭环

交付：

- 数据源详情「基于此表创建语义模型」，预填时间字段与维度。
- 模型创建后的指标注册引导（原子/派生）。
- 前端将「上传 → 建模型 → 注册指标 → 问数」串成连续旅程。

验收：从一张用户上传的表创建语义模型并注册指标后，Agent 可立即用该模型问数；语义模型页可切换查看、设为当前模型。

### M3：评测联动

交付：

- `run-data-evals.mjs` 对当前用户模型独立重算比对。
- 用户模型 schema 纳入评测运行快照。
- 数据工作区与评测页展示用户模型的数据准确率结果。

验收：激活用户模型后运行评测，data-accuracy 以用户表为基准输出准确率、查询耗时与数值比对数；运行历史可回溯用户模型快照。

### M4：多模型自动路由

交付：

- 跨模型指标/维度匹配，命中返回 `(modelId, metricKey)`。
- 唯一命中自动路由、多模型命中优先级、歧义澄清。
- 多轮上下文 `modelId` 继承。
- 查询工具与 LLM 目录工具支持 `modelId`。

验收：提问只存在于模型 B 的指标时自动使用模型 B；同名指标在多模型时优先当前模型、歧义时澄清；追问继承同一模型；确定性路径与 LLM 路径均覆盖。

### M5：Release Readiness

交付：

- README 更新（数据接入与自动路由的能力描述）。
- Changelog 与 v0.3.0 说明。
- 示例数据与端到端回归。
- 评测、测试、健康检查与审计门禁通过。

验收：全新 clone 可复现"上传 CSV → 建模型 → 注册指标 → 问数 → 评测"完整旅程。

## 9. 发布门槛

- 从全新 clone 可以在 10 分钟内完成一次"上传数据 → 建模型 → 问数 → 评测"。
- 上传建表：支持 CSV/XLSX，类型推断准确，微调生效，大小/行数限制与安全校验生效。
- 删除保护：被引用数据源删除返回明确错误。
- 自动路由：唯一命中路由准确率 100%；多模型歧义时进入澄清；上下文 modelId 继承准确率 100%。
- 既有能力不回归：v0.2 全部评测与测试继续通过。

## 10. 范围外与待办

- SQLite 文件导入（上传 `.db` 文件、选表复制）：MVP 不做，待办。
- DuckDB 连接器（本地大数据/Parquet）：待办。
- PostgreSQL / MySQL 等网络数据库连接器：待办（含凭据管理、连接配置 UI、查询代理）。
- 向量检索与重排序、数据质量规则引擎、可视化分析：待办。
- 任意 SQL 访问：永不放开。

## 11. 进度记录

### 2026-08-27：计划建立

- 状态：Completed
- 完成：确定 v0.3 版本主题、范围、设计决策、阶段计划与发布门槛。
- 下一步：执行 M1，开发数据工作区与 CSV/Excel 上传建表。
- 提交：`9c383a2`。

### 2026-08-27：M1 Data Workspace

- 状态：Completed，等待确认后进入 M2。
- 实际交付：
  - 迁移 `data/migrations/009_data_sources.sql`：`data_sources` 表（builtin/uploaded 两类）并预置内置数据源 `daily_metrics`。
  - `lib/data-source.mjs` `DataSourceService`：CSV/XLSX 解析（复用既有解析能力）、列类型与角色自动推断（时间/维度/数值/属性）、上传预览、安全建表（内部表名 `user_<uuid>`、列名白名单清洗）、批量事务写入、数据源列表/详情/预览、删除保护（内置源与已被语义模型引用的源返回 409）。
  - 接口：`POST /api/data/sources/preview`、`POST /api/data/sources`、`GET /api/data/sources`、`GET /api/data/sources/:id`、`DELETE /api/data/sources/:id`；`data_sources` 加入语义层候选表排除列表，用户表进入可建模型候选。
  - 前端：左侧新增「数据」入口；数据工作区（上传拖拽区、一键体验示例、数据源列表）、上传预览与列调整（列名/类型/角色）、创建数据表、数据源详情（列结构、数据预览、删除、指向语义模型的入口）。
  - 示例数据：`examples/data/sample-sales.csv`（12 行日期/地区/渠道/收入/订单/访客）。
- 验证：66/66 自动化测试通过（新增 `test/data-source.test.mjs` 8 条：迁移与内置源、候选表排除、CSV/XLSX 推断、建表插入、列表/详情/删除、内置与引用保护、HTTP 端点）；健康检查 22 实体 / 28 文档 / 0 error；敏感信息审计通过；真实服务端到端验证上传 → 推断 → 建表 → 详情 → 进入语义模型候选。
- 已知边界：上传后仍需在语义模型页手动创建模型并注册指标（M2 将补"基于此表一键建模型"引导）；未做大数据量（>10 万行）与编码探测（UTF-8 优先）。
- 阶段实现提交：`7eee3be`。
- 下一步：等待确认。确认后进入 M2，开发语义模型注册闭环（数据表 → 建模型 → 注册指标引导）。

### 2026-08-27：M2 Semantic Model Registration Loop

- 状态：Completed，等待确认后进入 M3。
- 实际交付：
  - 跨模型指标视图：`SemanticLayer.listAllMetrics()` 返回全部模型的指标并携带 `modelId/modelLabel/modelActive`；新增 `GET /api/semantic/metrics`。「指标」页改为模型感知——模型筛选（全部模型 + 各模型页签，当前模型标注）、指标行展示**所属语义模型**标签、同名指标在不同模型下可区分；指标详情面板显示所属模型。
  - 一键注册闭环：数据源详情「基于此表创建语义模型」→ 语义模型页带 `?table=` 参数自动打开「新增语义模型」对话框并预选该事实表 → 创建成功后自动弹出「注册第一个指标」；语义模型页在模型无指标时显示注册引导卡。
- 验证：66/66 自动化测试通过（语义层多模型用例补充 `listAllMetrics` 归属断言）；健康检查与审计通过；真实服务端到端验证 上传 → 基于用户表建模型 → 注册指标 → 激活 → 指标页归属正确（`revenue` 归内置模型、`sales_revenue` 归用户模型且标记当前）、被引用数据源删除返回 409。
- 已知边界：`showMetricContext` 的本体关联仍按 `metric-<key>` 全局页查找（多模型同名指标的本体页归属未按模型隔离）；指标详情点击属于用户接受的暂缓项。
- 阶段实现提交：`40989ca`。
- 下一步：等待确认。确认后进入 M3，开发评测联动（data-accuracy 覆盖用户模型）。

### 2026-08-27：M3 Evaluation Integration

- 状态：Completed，等待确认后进入 M4。
- 实际交付：
  - 数据准确率报告补充 `modelLabel`（`run-data-evals.mjs`），评测页数据准确率卡片展示「数值比对 · 所属模型标签」。
  - `DataSourceService.get` 返回引用该数据源的语义模型列表（id/label/active）；数据源详情新增「关联语义模型」区块，列出模型并提供「打开模型」入口（语义模型页支持 `?focus=<modelId>` 预选），注明 data-accuracy 对当前模型执行。
  - 确认数据准确率套件完整支持用户模型：`run-data-evals.mjs` 通过 `new SemanticLayer(undefined, db)` 加载活动模型、显式传递 `modelId`、直接读取模型事实表独立重算并逐项比对。
- 验证：66/66 自动化测试通过（数据源详情关联模型断言）；健康检查与审计通过；真实服务端到端验证 上传 → 基于用户表建模型 → 注册指标 → 激活 → `npm run eval:data` 以用户模型为基准输出 6/6 数值检查 100%、平均/P95 查询耗时，报告携带 `modelId=用户模型 / modelLabel`；运行快照经 `modelSnapshot()` 记录当前模型 schema。
- 已知边界：数据源详情的「数据准确率评测」列暂为说明文字，精确到模型的结果入口将在评测历史完善后补强；Judge/多轮等套件仍基于内置评测集，不直接消费用户数据。
- 阶段实现提交：`dacf16f`。
- 下一步：等待确认。确认后进入 M4，开发多模型自动路由（跨模型匹配、上下文 modelId、澄清）。

### 2026-08-27：M4 Multi-model Auto Routing

- 状态：Completed，等待确认后进入 M5。
- 实际交付：
  - 跨模型指标匹配：`SemanticLayer.findMetricsAcrossModels(text)` 在全部模型中按**位置做全局消歧**（较长术语覆盖重叠的较短术语，避免「单量」误吞「销售单量」），命中返回 `(modelId, key, label, modelActive)`；`findMetrics/findDimensions` 支持按模型查询。
  - 自动路由：`AgentRuntime.resolve` 重写——术语唯一命中某模型即自动路由；多模型命中时优先会话上下文 `modelId`、其次当前激活模型；仍有多义则进入 `ambiguity`（模型选项）。追问（未命名指标）继承上下文 `modelId`。
  - 上下文持久化：迁移 `010_conversation_model.sql` 为 `conversation_context` 增加 `model_id`；`ConversationService` 的 load/save/merge 与运行后 `contextAfter` 均携带 `modelId`。
  - 工具与 LLM：`metric_query`、`compare_periods`、`dimension_breakdown`、`semantic_catalog` 的输入契约与实现支持 `modelId`（底层 `buildQuery` 原本已接受）；确定性路径按所选模型取标签/维度/格式。
  - 澄清：模型歧义时 `clarificationFor` 返回模型选项（`kind:"model"`），`resolveClarification` 按 kind 应用 `modelId` 或指标；指标澄清保留原有行为。
  - 自定义模型维度补充常见中文别名（region→地区/区域/大区、channel→渠道/来源、category→品类 等），使「按地区拆」等中文追问在用户模型上可命中。
- 验证：70/70 自动化测试通过（新增 `test/multi-model-routing.test.mjs` 4 条：唯一模型自动路由、追问继承模型与维度、跨模型歧义澄清恢复、激活模型优先）；健康检查与审计通过；真实服务端到端验证 用户模型未激活时提问其独有指标「销售单量」自动路由到该模型、追问「那按地区拆一下」继承模型并命中维度 `region`。
- 已知边界：维度路由仍以指标为主信号（仅出现维度术语时回退当前模型）；跨模型同名指标的本体页（`metric-<key>`）仍未按模型隔离。
- 阶段实现提交：`2ed196a`。
- 下一步：等待确认。确认后进入 M5，完成 v0.3 发布检查（README 数据接入与自动路由能力、Changelog、v0.3.0、端到端回归）。

### 2026-08-27：M5 Release Readiness

- 状态：Completed，v0.3.0 发布。
- 实际交付：
  - README 重写为「从数据到答案的完整旅程」：新增「接入自己的数据（Data）」能力章节（CSV/Excel 上传建表、数据工作区、删除保护、示例数据），「提问与分析」补充 AI 自动选择语义模型说明；快速开始新增"接入自己的数据"体验步骤；演进方向更新为已交付 v0.3 后的连接器/治理/检索路线。
  - CHANGELOG 新增 `[0.3.0]` 条目（数据工作区、多语义模型、跨模型自动路由、版本化评测与 Judge/数据准确率/耗时指标、安全边界），并清理 Unreleased。
  - `package.json` 版本提升至 `0.3.0`。
  - 示例数据 `examples/data/sample-sales.csv` 已纳入端到端体验路径。
- 验证：`npm run verify` 完整门禁通过——70/70 自动化测试；健康检查 22 实体 / 28 文档 / 0 error；120 单轮、30 组多轮、15 Wiki 专项、数据准确率 35/35（100%）全部通过；LLM Judge 在未配置密钥时优雅输出 `not_configured`；敏感信息审计与 `npm audit`（0 漏洞）通过。
- 阶段实现提交：`3f3a5f5`（含 `v0.3.0` tag）。
- 下一步：v0.3 发布完成。后续方向见 README「设计边界与演进方向」（数据库连接器、企业化治理、检索增强等），并等待新迭代规划。

### 2026-08-27：真实模型链路验证（deepseek-v4-flash）

- 状态：Completed。
- 接入：`.env` 配置 DeepSeek 网关（`LLM_BASE_URL=https://api.deepseek.com/v1`、`LLM_MODEL=deepseek-v4-flash`、用户提供 API Key，`.env` 不入库）。
- 修复（真实链路暴露的问题）：
  - **确定性回退预算**：LLM 耗尽其工具预算后，回退路径曾在同一 `calls` 上继续计数导致"超出最大工具步数"；修复为回退前重置 `calls`/`sources`。
  - **相对日期解析**：LLM 不知道"今天"，曾把"近 14 天"猜成 2025/2024 年份；系统提示注入今天日期并规定「近 N 天 → [今天-N+1, 今天]」解析规则。
  - **LLM 预算余量**：comparative-analysis 6→8、metric-query 5→6、wiki-answer 5→6；提示词补充"已完成的查询不要重复"。
- 验证（provider 均为 `llm`，无回退）：
  - 口径问答（wiki-answer）：13s，回答「客单价 = SUM(收入)/SUM(订单量)」正确。
  - 单值问数（metric-query）：6s，近 14 天收入 1,721,905.29，日期范围正确。
  - 对比分析（comparative-analysis）：31s，输出日粒度趋势表 + 期间对比 + 维度拆分。
  - 多轮追问「那按地区拆一下」：继承模型/指标/维度，按地区拆分正确。
  - LLM-as-a-Judge：6/6 条评分，总分 84.2/100，四维（正确性 0.87 / 忠实性 0.87 / 完整性 0.83 / 引用质量 0.80），Agent 平均耗时约 7s。
  - 70/70 自动化测试通过；健康检查与敏感信息审计通过。
- 已知边界：LLM 路径单轮耗时约 6–31s（工具多轮往返）；分析类问题仍可能出现轻微冗余调用；评测套件（单轮/多轮/Wiki/数据）默认 `FORCE_DETERMINISTIC=1` 不受 LLM 影响，仅 Judge 使用模型。
- 阶段实现提交：`e46bf3d`。
- 下一步：真实链路可用；可在此基础上评估是否开启 v0.4 规划（数据库连接器、检索增强、多用户治理等）。
