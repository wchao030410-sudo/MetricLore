# 数据语义层

数据语义层把业务概念映射为受治理的指标、维度、聚合方式和物理字段。用户与 Agent 查询“收入”时，由语义层生成受控 SQL，而不是让模型直接猜测表名和字段。

本项目使用 `config/semantic-model.json` 作为唯一目录。所有指标和维度都必须预先注册；请求只能选择白名单对象，筛选值通过参数绑定进入 SQLite。

这种边界能降低口径漂移和 SQL 注入风险，也方便为每次查询记录确定的指标版本。
