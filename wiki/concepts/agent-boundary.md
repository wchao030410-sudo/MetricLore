# MetricLore 的职责边界

MetricLore 负责理解问题、选择 Wiki 检索或指标查询，并把证据组织成回答。确定性代码负责指标白名单、日期、SQL 生成、参数绑定和结果限制。

Agent 不应把趋势相关性写成因果关系。用户询问“为什么下降”时，如果只有指标序列，应先描述变化，并说明还需要价格、活动、供给或渠道策略等证据。

模型只能接收服务端提供的 Wiki 摘要与查询结果，不获得任意 SQL 执行工具。
