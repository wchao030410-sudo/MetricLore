# 订阅业务知识包

这套合成示例展示 Markdown 业务知识与 SQL 数据结构的联合摄入。Wiki Builder 会从 Markdown 生成指标和业务过程候选，并从建表语句生成数据资产与字段候选。

## 导入

1. 打开「Wiki 构建」，选择本目录。
2. 在审核队列核对 MRR 与续费流程的关系，以及 SQL 抽取出的资产和字段。
3. 批准后发布，在本体图中聚焦 `metric-mrr`。

示例只描述虚构的订阅产品。

